//const API_BASE_URL = import.meta.env.VITE_API_URL || 'https://ideagora-backend.onrender.com'

//  Configura tu localhost (asegúrate de poner el puerto exacto que usa tu backend, ej: 5000 o 3000)
const API_BASE_URL = "http://localhost:5000"; 

export default API_BASE_URL;